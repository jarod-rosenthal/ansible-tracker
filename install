#!/bin/bash

apt install -y git ansible

if [ ! -d ansible-tracker ]; then
	git clone https://gitlab.com/skyhuborg/ansible-tracker.git
else
	pushd ansible-tracker
	git pull
	popd
fi

cd ansible-tracker
